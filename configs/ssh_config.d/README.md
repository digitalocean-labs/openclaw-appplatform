# SSH Client Config

Place SSH client configuration files here. They will be copied to `/etc/ssh/ssh_config.d/` in the container.

Example: Create a file like `10-defaults.conf` with client-side SSH options.
