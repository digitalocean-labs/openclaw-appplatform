# SSH Server Config

Place SSH server configuration files here. They will be copied to `/etc/ssh/sshd_config.d/` in the container.

Example: Create a file like `10-clawdbot.conf` with server-side SSH options.
