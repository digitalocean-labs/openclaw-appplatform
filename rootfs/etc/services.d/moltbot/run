#!/command/with-contenv bash
# Moltbot gateway service

echo "[moltbot] Starting moltbot gateway..."

# Source profile and run gateway (litestream runs as separate service)
exec s6-setuidgid moltbot bash -l -c 'exec moltbot gateway --allow-unconfigured'
